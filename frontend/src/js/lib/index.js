// api address  
const url = 'http://localhost:8080/api'

// endpoints
const diagram = '/diagram/' //require an argument
const diagrams = '/diagrams/'

const endpoints = {
    'diagram' : diagram,
    'diagrams' : diagrams,
}

export {
    url,
    endpoints
}